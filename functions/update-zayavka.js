const DEFAULT_NOCODB_ENDPOINT = "https://app.nocodb.com/api/v3/data/ptvxn8nmuwc08y3/mgp2zjsuv4id5tp/records";
function json(body,status=200){return new Response(JSON.stringify(body,null,2),{status,headers:{"Content-Type":"application/json; charset=utf-8"}})}
function parseJson(t){try{return JSON.parse(t)}catch(e){return t}}
function checkAdmin(request, env){const required=env.ADMIN_PASSWORD;if(!required)return {ok:false,status:500,body:{ok:false,error:"ADMIN_PASSWORD is not set"}};const provided=(request.headers.get("x-admin-password")||"").trim();if(provided!==required)return {ok:false,status:401,body:{ok:false,error:"Неверный пароль администратора"}};return {ok:true}}
function normRecord(rec){const fields=rec.fields||rec;const id=rec.id||(rec.id_fields&&(rec.id_fields.Id||rec.id_fields.id))||fields.Id||fields.id||"";return {id:String(id),fields}}

export async function onRequestPost(context){const {request,env}=context;const auth=checkAdmin(request,env);if(!auth.ok)return json(auth.body,auth.status);if(!env.NOCODB_TOKEN)return json({ok:false,error:"NOCODB_TOKEN is missing"},500);let body;try{body=await request.json()}catch(e){return json({ok:false,error:"Invalid JSON"},400)}if(!body.id)return json({ok:false,error:"Record id is required"},400);const allowed=["Статус","Итоговый м2","Ответственный","Комментарий администратора","Создан объект","Монтажники"];const fields={};for(const k of allowed){if(body.fields&&Object.prototype.hasOwnProperty.call(body.fields,k)){let v=body.fields[k];if(k==="Итоговый м2"){if(v===""||v===null||v===undefined)continue;v=Number(String(v).replace(",","."));if(!Number.isFinite(v))continue}fields[k]=v}}const payload=[{id:Number(body.id)||body.id,fields}];try{const res=await fetch(env.NOCODB_ENDPOINT||DEFAULT_NOCODB_ENDPOINT,{method:"PATCH",headers:{"Content-Type":"application/json","xc-token":env.NOCODB_TOKEN},body:JSON.stringify(payload)});const text=await res.text();const data=parseJson(text);if(!res.ok)return json({ok:false,error:"NocoDB update error",status:res.status,nocodbResponse:data,sentPayload:payload},500);return json({ok:true,nocodbResponse:data})}catch(e){return json({ok:false,error:e.message},500)}}
export async function onRequest(context){if(context.request.method!=="POST")return json({ok:false,error:"Only POST"},405);return onRequestPost(context)}
